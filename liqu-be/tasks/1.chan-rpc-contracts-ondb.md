# Task 01 - Chain RPC Contracts on DB 
so right now the contract address were in .env, i wanna move it to db, and also the rpc url, so we can add new chain and contract easily

## Goal
build necessary db prisma schema and its migration, also implement some logic, services, and endpoint to support it

## tasks
- [ ] create prisma table for chain and its information like chain id, etc to make easier integrate in frontend (we are using viem, rainbowkit in our frontend)
- [ ] create table for rpcs, one chain can have multiple rpcs, and we can add new rpc easily
- [ ] create table for contracts, one chain can have multiple contract and each (chainId, contractName, and isActive true) should be unique to mantain integrity
- [ ] crete endpoint to get list of chains
- [ ] for rpc frontend can hit to use (we're proxying rpc) and using like POST /v1/rpc/:chainId
- [ ] can list all contract address by chainId
- [ ] can get contract ABI by chainId and contract address -> abi can be saved in db in contracts table with json data type
- [ ] in chain table, please add isChain active or not, so we can disable chain easily
- [ ] if you already finish, please make docs or task document like this that i can pass to frontend to proceed it for integration
